# <effort name> — live status

_last updated: 2026-01-01_

> DELETE THIS FILE. Worked example of a living status tracker. Copy the shape.

## State
- `<one-line current state>` — e.g. "v2 deployed to dev+qa; prod rollout pending flag flip."

## PRs / branches
- #<n> (merged) — `<what it did>`
- #<n> (open) — `<what it does>` · branch `<name>`

## Blocked / awaiting
- `<item>` — awaiting `<who/what>` since `<date>`

## Evidence still outstanding (before "done")
- `<item>` — collect by `<command/query>`

## Related docs
- agent-plans/YYYY-MM-DD-<slug>.md
